/*
 * Defines the style of the tab summary
 */
export enum AddinTabSummaryStyle {
  // No style (default)
  None = 0,

  // Show summary text next to the tab caption (the value shown will be the 'summary' property)
  Text = 1
}
